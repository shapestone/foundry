# {{ .ProjectName }}

A REST API service built with Foundry.

## Quick Start

```bash
# Install dependencies
go mod tidy

# Run the service
go run .

# Test the API
curl http://localhost:8080/health
curl http://localhost:8080/api/hello
```

## Endpoints

- `GET /health` - Health check
- `GET /api/hello` - Example endpoint
